syntax = "proto3";

service LLMService {
    rpc InitiateSession (UserData) returns (MemStatus);
    rpc Chat (UserInput) returns (Response);
}
message UserData {
    string session_id = 1;
    string user_backg = 2;
}

message MemStatus {
    bool status = 1;
}

message UserInput {
    string user_id = 1;
    string message = 2;
}

message Response {
    string message = 1;
}